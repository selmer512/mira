name: Greenfield memory source patch

on:
  push:
    branches:
      - greenfield/vertical-slice-foundation-20260710
    paths:
      - 'scripts/greenfield/apply-memory-event-order-fix.mjs'

permissions:
  contents: write

concurrency:
  group: greenfield-memory-source-patch
  cancel-in-progress: false

jobs:
  patch:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out feature branch
        uses: actions/checkout@v4
        with:
          ref: greenfield/vertical-slice-foundation-20260710
          fetch-depth: 0

      - name: Apply exact append-order fix
        run: node scripts/greenfield/apply-memory-event-order-fix.mjs

      - name: Commit exact source correction
        run: |
          git config user.name 'mira-greenfield-bot'
          git config user.email 'mira-greenfield-bot@users.noreply.github.com'
          git add server/src/core/greenfield/memory-store.ts
          git add -u .github/workflows/greenfield-memory-source-patch.yml scripts/greenfield/apply-memory-event-order-fix.mjs
          git diff --cached --check
          git commit -m 'fix(memory): verify event chain in append order'
          git push origin HEAD:greenfield/vertical-slice-foundation-20260710
