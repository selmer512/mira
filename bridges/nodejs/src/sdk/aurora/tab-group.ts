import { type TabGroupProps } from '@mira-ai/aurora'

import { WidgetComponent } from '../widget-component'

export class TabGroup extends WidgetComponent<TabGroupProps> {
  constructor(props: TabGroupProps) {
    super(props)
  }
}
